# 🚀 Инструкция по загрузке проекта на GitHub

## 📋 Пошаговая инструкция

### Шаг 1: Проверка и подготовка проекта

#### 1.1. Проверьте .gitignore
Убедитесь, что в `.gitignore` есть все необходимые исключения:

```bash
# Должны быть исключены:
- .venv/ или venv/
- .idea/
- .env
- __pycache__/
- *.db
- uploads/
- output/
```

✅ Файл `.gitignore` уже настроен правильно!

#### 1.2. Проверьте что не будет загружено
```bash
# Перейдите в папку проекта
cd "C:\Users\user\Desktop\Cliner finance"

# Проверьте статус (если Git уже инициализирован)
git status
```

---

### Шаг 2: Инициализация Git (если ещё не сделано)

```bash
# Перейдите в папку проекта
cd "C:\Users\user\Desktop\Cliner finance"

# Инициализация Git репозитория
git init

# Проверка статуса
git status
```

---

### Шаг 3: Настройка Git (если ещё не настроено)

```bash
# Установите ваше имя (если ещё не установлено)
git config --global user.name "Ваше Имя"

# Установите ваш email (если ещё не установлен)
git config --global user.email "your.email@example.com"

# Проверка настроек
git config --list
```

---

### Шаг 4: Добавление файлов

```bash
# Добавить все файлы (кроме тех что в .gitignore)
git add .

# Проверьте что будет закоммичено
git status
```

**⚠️ ВАЖНО:** Убедитесь, что НЕ добавляются:
- ❌ `.env` файл
- ❌ `.venv/` или `venv/` папка
- ❌ `.idea/` папка
- ❌ `credit_ai.db` файл
- ❌ `uploads/` и `output/` папки

---

### Шаг 5: Первый коммит

```bash
# Создайте первый коммит
git commit -m "Initial commit: CreditAI system for credit report processing"
```

---

### Шаг 6: Создание репозитория на GitHub

#### 6.1. Войдите в GitHub
1. Откройте https://github.com
2. Войдите в свой аккаунт (или создайте новый)

#### 6.2. Создайте новый репозиторий
1. Нажмите кнопку **"+"** в правом верхнем углу
2. Выберите **"New repository"**

#### 6.3. Заполните данные репозитория:
- **Repository name:** `credit-ai` (или другое имя)
- **Description:** `Система автоматической обработки кредитных отчётов с использованием ИИ`
- **Visibility:** 
  - ✅ **Private** (рекомендуется для коммерческого проекта)
  - или **Public** (если хотите открытый код)
- **НЕ отмечайте:**
  - ❌ "Add a README file" (у нас уже есть)
  - ❌ "Add .gitignore" (у нас уже есть)
  - ❌ "Choose a license" (можно добавить позже)

4. Нажмите **"Create repository"**

#### 6.4. Скопируйте URL репозитория
GitHub покажет инструкции. Скопируйте URL, например:
```
https://github.com/your-username/credit-ai.git
```
или для SSH:
```
git@github.com:your-username/credit-ai.git
```

---

### Шаг 7: Подключение к GitHub и загрузка

```bash
# Добавьте удалённый репозиторий
# Замените URL на ваш реальный URL из GitHub
git remote add origin https://github.com/your-username/credit-ai.git

# Проверка подключения
git remote -v

# Загрузите код на GitHub
git push -u origin main
```

**Если ветка называется `master` вместо `main`:**
```bash
# Переименуйте ветку
git branch -M main

# Или используйте master
git push -u origin master
```

**Если потребуется авторизация:**
- GitHub больше не поддерживает пароли для HTTPS
- Используйте **Personal Access Token** (см. ниже) или **SSH ключи**

---

### Шаг 8: Настройка авторизации (если нужно)

#### Вариант A: Personal Access Token (HTTPS)

1. **Создайте токен:**
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Нажмите "Generate new token (classic)"
   - Выберите срок действия и права доступа (repo)
   - Скопируйте токен (показывается только один раз!)

2. **Используйте токен вместо пароля:**
   ```bash
   git push -u origin main
   # Username: ваш_username
   # Password: вставьте токен (не пароль!)
   ```

#### Вариант B: SSH ключи (рекомендуется)

1. **Проверьте есть ли SSH ключ:**
   ```bash
   ls ~/.ssh
   # Должен быть id_rsa.pub или id_ed25519.pub
   ```

2. **Если нет, создайте:**
   ```bash
   ssh-keygen -t ed25519 -C "your.email@example.com"
   # Нажмите Enter для всех вопросов
   ```

3. **Скопируйте публичный ключ:**
   ```bash
   # Windows (PowerShell)
   cat ~/.ssh/id_ed25519.pub
   
   # Или откройте файл в блокноте:
   # C:\Users\user\.ssh\id_ed25519.pub
   ```

4. **Добавьте ключ в GitHub:**
   - GitHub → Settings → SSH and GPG keys → New SSH key
   - Вставьте содержимое файла `.pub`
   - Сохраните

5. **Используйте SSH URL:**
   ```bash
   git remote set-url origin git@github.com:your-username/credit-ai.git
   git push -u origin main
   ```

---

## ✅ Проверка успешной загрузки

1. Откройте ваш репозиторий на GitHub
2. Проверьте что все файлы загружены
3. Убедитесь что НЕТ:
   - ❌ `.env` файла
   - ❌ `.venv/` папки
   - ❌ `credit_ai.db` файла

---

## 🔄 Дальнейшая работа с репозиторием

### Добавление изменений:

```bash
# Проверка изменений
git status

# Добавление файлов
git add .

# Коммит
git commit -m "Описание изменений"

# Загрузка на GitHub
git push
```

### Получение изменений:

```bash
# Скачать изменения с GitHub
git pull
```

### Создание новой ветки:

```bash
# Создать и переключиться на новую ветку
git checkout -b feature/new-feature

# Загрузить ветку на GitHub
git push -u origin feature/new-feature
```

---

## ⚠️ Важные моменты

### 1. НЕ коммитьте секретные данные:
- ❌ `.env` файл
- ❌ Пароли и API ключи в коде
- ❌ Персональные данные

### 2. Используйте `env.example`:
- ✅ Коммитьте `env.example` как шаблон
- ❌ НЕ коммитьте реальный `.env`

### 3. Проверяйте перед коммитом:
```bash
git status
# Убедитесь что нет .env, .venv и т.д.
```

### 4. Если случайно закоммитили секреты:
```bash
# Удалить из истории (осторожно!)
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env" \
  --prune-empty --tag-name-filter cat -- --all

# Или используйте BFG Repo-Cleaner (проще)
```

---

## 📝 Рекомендуемая структура коммитов

### Первый коммит:
```
Initial commit: CreditAI system for credit report processing
```

### Типичные коммиты:
```
feat: Add PDF processing module
fix: Fix BKI detection for ОКБ format
docs: Update installation guide
refactor: Improve database repository
test: Add unit tests for validators
```

---

## 🎯 Быстрая команда (если всё готово)

Если Git уже инициализирован и файлы готовы:

```bash
cd "C:\Users\user\Desktop\Cliner finance"
git add .
git commit -m "Initial commit: CreditAI system"
git remote add origin https://github.com/your-username/credit-ai.git
git branch -M main
git push -u origin main
```

---

## 🆘 Решение проблем

### Проблема: "remote origin already exists"
```bash
# Удалить старый remote
git remote remove origin

# Добавить заново
git remote add origin https://github.com/your-username/credit-ai.git
```

### Проблема: "Authentication failed"
- Используйте Personal Access Token вместо пароля
- Или настройте SSH ключи

### Проблема: "Failed to push some refs"
```bash
# Сначала получите изменения
git pull origin main --allow-unrelated-histories

# Затем загрузите
git push -u origin main
```

### Проблема: Большой размер репозитория
```bash
# Проверьте что не закоммичены большие файлы
git ls-files | xargs ls -lh | sort -k5 -hr | head -20

# Если есть .venv или другие большие файлы:
git rm -r --cached .venv
git commit -m "Remove .venv from repository"
```

---

## ✅ Чеклист перед загрузкой

- [ ] `.gitignore` настроен правильно
- [ ] `.env` файл НЕ будет закоммичен
- [ ] `.venv/` папка НЕ будет закоммичена
- [ ] `credit_ai.db` НЕ будет закоммичен
- [ ] Все файлы проверены через `git status`
- [ ] Репозиторий создан на GitHub
- [ ] URL репозитория скопирован
- [ ] Авторизация настроена (токен или SSH)

---

**Готово! 🎉** Ваш проект теперь на GitHub!

