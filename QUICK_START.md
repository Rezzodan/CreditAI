# ⚡ Быстрый старт CreditAI

**За 5 минут до первого отчёта!**

---

## 1️⃣ Установка зависимостей (2 минуты)

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

---

## 2️⃣ Настройка Ollama (3 минуты)

```bash
# Установите Ollama (если ещё не установлен)
# https://ollama.com/download

# Скачайте модель
ollama pull qwen2.5:1.5b

# Проверьте
ollama list
```

---

## 3️⃣ Запуск сервера (30 секунд)

```bash
# Инициализируйте базу данных
python init_db.py

# Запустите сервер
python main.py
```

Откройте: http://localhost:8000/docs

---

## 4️⃣ Первый тест (1 минута)

### Вариант А: Через Swagger UI

1. Откройте http://localhost:8000/docs
2. Найдите `/api/process`
3. Нажмите "Try it out"
4. Загрузите PDF кредитного отчёта
5. Нажмите "Execute"
6. Получите результат!

### Вариант Б: Через curl

```bash
curl -X POST http://localhost:8000/api/process \
  -F "file=@credit_report.pdf"
```

---

## ✅ Готово!

Если видите `"status": "completed"` - система работает! 🎉

---

## 🔧 Настройка (опционально)

Создайте файл `.env`:

```bash
# Скопируйте пример
cp env.example .env

# Отредактируйте настройки
# Минимальные настройки:
OLLAMA_MODEL="qwen2.5:1.5b"
DATABASE_URL="sqlite:///./credit_ai.db"
```

---

## 📊 Генерация отчёта по ТЗ

```bash
# После обработки PDF
POST /api/process_tz
Content-Type: multipart/form-data

file: <кредитный_отчет.pdf>
client_name: "Иванов Иван Иванович"
```

**Результат:** Word отчёт с рекомендацией тарифа (Премиум/Оптимум)

---

## 🐛 Решение проблем

### Ошибка: "Module not found"
```bash
pip install -r requirements.txt
```

### Ошибка: "Ollama connection refused"
```bash
ollama serve
```

### Ошибка: "Database error"
```bash
python init_db.py
```

---

## 📚 Дополнительно

- **Полная документация:** `README.md`
- **Настройка Ollama:** `OLLAMA_SETUP.md`
- **Отчёты по ТЗ:** `TZ_IMPLEMENTATION_GUIDE.md`
- **Статус проекта:** `TZ_READY.md`

---

**Вопросы?** Смотрите документацию или проверьте логи в `logs/`



