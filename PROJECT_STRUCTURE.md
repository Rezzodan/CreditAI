# Структура проекта и объяснение папок

## 📁 Основные папки проекта

### `/api/` - API слой
FastAPI эндпоинты и роутинг
- `main.py` - основное приложение FastAPI
- `endpoints.py` - все API эндпоинты

### `/config/` - Конфигурация
Настройки приложения и промпты для ИИ
- `settings.py` - настройки через Pydantic
- `prompts.py` - промпты для Ollama

### `/core/` - Ядро системы
Основная бизнес-логика
- `pdf_processor.py` - обработка PDF файлов
- `ai_processor.py` - работа с Ollama API
- `bki_detector.py` - определение типа БКИ
- `data_validator.py` - валидация данных

### `/database/` - База данных
Модели и репозитории
- `models.py` - SQLAlchemy модели
- `repository.py` - CRUD операции

### `/services/` - Сервисы
Вспомогательные сервисы
- `document_generator.py` - генерация Word документов

### `/integration/` - Интеграции
Интеграция с внешними сервисами
- `bitrix_client.py` - клиент Битрикс24
- `celery_tasks.py` - фоновые задачи

### `/utils/` - Утилиты
Вспомогательные функции
- `logger.py` - настройка логирования

---

## 🔒 Скрытые/Системные папки (не коммитятся в Git)

### `.venv/` или `venv/` - Виртуальное окружение Python

**Что это:**
- Изолированная среда Python для проекта
- Содержит установленные пакеты из `requirements.txt`
- Создаётся командой: `python -m venv venv` или `python -m venv .venv`

**Зачем нужно:**
- Изоляция зависимостей проекта от системного Python
- Разные версии пакетов для разных проектов
- Чистота окружения

**Нужно ли коммитить в Git:**
❌ **НЕТ!** Эта папка должна быть в `.gitignore`

**Размер:**
- Обычно 100-500 MB (зависит от установленных пакетов)

**Когда создаётся:**
- При первом запуске: `python -m venv venv`
- Автоматически некоторыми IDE

---

### `.idea/` - Настройки PyCharm/IntelliJ IDEA

**Что это:**
- Папка с настройками IDE PyCharm или IntelliJ IDEA
- Содержит конфигурации запуска, настройки интерпретатора, стили кода и т.д.

**Зачем нужно:**
- Хранение персональных настроек IDE
- Конфигурации запуска и отладки
- Настройки форматирования кода

**Нужно ли коммитить в Git:**
❌ **Обычно НЕТ!** Это персональные настройки IDE

**Исключения:**
- Если команда хочет разделять общие настройки IDE
- В этом случае можно коммитить некоторые файлы (но не все)

**Размер:**
- Обычно 1-10 MB

**Когда создаётся:**
- При открытии проекта в PyCharm/IntelliJ IDEA

---

## 📝 Другие важные файлы и папки

### `.env` - Переменные окружения
❌ **НЕ коммитится!** Содержит секретные ключи и пароли
- Используйте `env.example` как шаблон

### `__pycache__/` - Кэш Python
❌ **НЕ коммитится!** Скомпилированные .pyc файлы
- Создаётся автоматически при запуске Python

### `uploads/` - Загруженные файлы
❌ **НЕ коммитится!** Пользовательские PDF файлы
- Создаётся автоматически при загрузке файлов

### `output/` - Сгенерированные документы
❌ **НЕ коммитится!** Сгенерированные Word документы
- Создаётся автоматически при генерации документов

### `credit_ai.db` - База данных SQLite
❌ **НЕ коммитится!** (для MVP)
- Создаётся при запуске `python init_db.py`

---

## ✅ Что должно быть в Git

### Обязательно коммитить:
- ✅ Весь исходный код (`.py` файлы)
- ✅ `requirements.txt` - зависимости
- ✅ `README.md`, документация
- ✅ `Dockerfile`, `docker-compose.yml`
- ✅ `.gitignore` - правила игнорирования
- ✅ `env.example` - шаблон конфигурации

### НЕ коммитить:
- ❌ `.venv/`, `venv/` - виртуальное окружение
- ❌ `.idea/`, `.vscode/` - настройки IDE
- ❌ `.env` - секретные данные
- ❌ `__pycache__/` - кэш Python
- ❌ `*.db`, `*.sqlite` - базы данных
- ❌ `uploads/`, `output/` - пользовательские файлы
- ❌ `*.log` - логи

---

## 🔍 Как проверить что правильно настроено

### Проверка .gitignore:
```bash
# Проверка что папки игнорируются
git status
# .venv и .idea не должны отображаться
```

### Проверка размера репозитория:
```bash
# Если репозиторий слишком большой (>50MB), возможно что-то не игнорируется
du -sh .git
```

### Очистка уже закоммиченных файлов:
Если случайно закоммитили `.venv` или `.idea`:

```bash
# Удалить из индекса (но оставить локально)
git rm -r --cached .venv
git rm -r --cached .idea

# Закоммитить изменения
git commit -m "Remove .venv and .idea from repository"
```

---

## 📊 Типичная структура проекта

```
Cliner finance/
├── api/                    ✅ Код
├── config/                 ✅ Код
├── core/                   ✅ Код
├── database/               ✅ Код
├── services/               ✅ Код
├── integration/            ✅ Код
├── utils/                  ✅ Код
├── .venv/                  ❌ Игнорируется
├── .idea/                  ❌ Игнорируется
├── __pycache__/            ❌ Игнорируется
├── uploads/                ❌ Игнорируется
├── output/                 ❌ Игнорируется
├── credit_ai.db           ❌ Игнорируется
├── .env                   ❌ Игнорируется
├── .gitignore             ✅ В Git
├── requirements.txt       ✅ В Git
├── README.md              ✅ В Git
└── ...                    ✅ Документация
```

---

## 🛠️ Рекомендации

### Для разработчиков:

1. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   # или
   python -m venv .venv
   ```

2. **Активируйте его:**
   ```bash
   venv\Scripts\activate  # Windows
   source venv/bin/activate  # Linux/Mac
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Не коммитьте `.venv` и `.idea`:**
   - Они уже в `.gitignore`
   - Каждый разработчик создаёт своё окружение

### Для IDE:

**PyCharm:**
- `.idea/` создаётся автоматически
- Можно настроить общие настройки через `.idea/codeStyles/`
- Но обычно лучше не коммитить

**VS Code:**
- Настройки в `.vscode/`
- Можно коммитить `settings.json` для общих настроек команды

---

## ❓ Частые вопросы

**Q: Можно ли удалить `.venv`?**
A: Да, это можно пересоздать командой `python -m venv venv`

**Q: Нужно ли коммитить `.idea/`?**
A: Обычно нет, это персональные настройки. Но можно коммитить некоторые общие настройки команды.

**Q: Что делать если `.venv` уже в Git?**
A: Удалите из индекса: `git rm -r --cached .venv` и добавьте в `.gitignore`

**Q: Разные имена: `venv` vs `.venv`?**
A: Оба варианта правильные. `.venv` - скрытая папка (точка в начале). Оба должны быть в `.gitignore`.


