# ⚡ Шпаргалка по командам CreditAI

**Самые частые команды для ежедневной работы**

---

## 🚀 БЫСТРЫЙ СТАРТ

```bash
cd "C:\Users\user\Desktop\Cliner finance"
venv\Scripts\activate              # ⚠️ ОБЯЗАТЕЛЬНО активируйте venv!
python main.py
```

**→ Откройте:** http://localhost:8000/docs

---

## ⚠️ ВАЖНО: ВИРТУАЛЬНОЕ ОКРУЖЕНИЕ

**На НОВОМ компьютере сначала:**
1. Создайте venv: `python -m venv venv`
2. Установите библиотеки: `pip install -r requirements.txt`
3. **Подробно:** См. файл `УСТАНОВКА_VENV.md`
4. **Автоматически:** Запустите `установка.bat`

---

## 📋 ЕЖЕДНЕВНАЯ РАБОТА

### Запуск проекта:
```bash
venv\Scripts\activate       # ⚠️ ВСЕГДА активируйте venv первым делом!
python main.py              # Запустить сервер
```

### Остановка:
```
Ctrl + C                    # Остановить сервер
deactivate                  # Деактивировать venv (опционально)
```

### Проверка что venv активен:
```bash
# Должен быть префикс (venv) в терминале:
(venv) C:\Users\user\Desktop\Cliner finance>
      ↑ Этот префикс означает что venv активен!
```

---

## 🤖 OLLAMA

```bash
ollama list                 # Посмотреть модели
ollama serve                # Запустить Ollama
ollama pull qwen2.5-coder:1.5b  # Загрузить модель
```

---

## 🐙 GIT

```bash
git status                  # Проверить статус
git add .                   # Добавить все файлы
git commit -m "описание"    # Закоммитить
git push                    # Отправить на GitHub
git pull                    # Получить обновления
```

---

## 📡 API (curl)

```bash
# Проверка здоровья
curl http://localhost:8000/health

# Загрузка PDF
curl -X POST -F "file=@test.pdf" http://localhost:8000/api/v1/process

# Статус обработки
curl http://localhost:8000/api/v1/status/report_id

# Статистика
curl http://localhost:8000/api/v1/stats
```

---

## 🗄️ БАЗА ДАННЫХ

```bash
python init_db.py           # Создать/пересоздать БД
del credit_ai.db            # Удалить БД
```

---

## 🐳 DOCKER

```bash
docker-compose up           # Запустить всё
docker-compose down         # Остановить всё
docker-compose logs -f      # Смотреть логи
```

---

## 📦 ЗАВИСИМОСТИ

```bash
pip install -r requirements.txt  # Установить всё
pip list                         # Список пакетов
pip freeze > requirements.txt    # Обновить список
```

---

## 🔍 ОТЛАДКА

```bash
type .env                   # Посмотреть конфигурацию
type logs\creditai.log      # Посмотреть логи
ollama list                 # Проверить модели
python --version            # Версия Python
```

---

## 📁 ОЧИСТКА

```bash
del credit_ai.db            # Удалить БД
del logs\*.log              # Удалить логи
del /q uploads\*            # Очистить uploads
del /q output\*             # Очистить output
```

---

## 🎯 ЧАСТЫЕ СЦЕНАРИИ

### Утром (начало работы):
```bash
cd "C:\Users\user\Desktop\Cliner finance"
venv\Scripts\activate
git pull
python main.py
```

### Вечером (сохранение изменений):
```bash
git add .
git commit -m "Описание работы за день"
git push
```

### Проблемы? Перезапуск:
```bash
# Ctrl+C для остановки
del credit_ai.db
python init_db.py
python main.py
```

---

## 🔗 ССЫЛКИ

- **API Docs:** http://localhost:8000/docs
- **Health:** http://localhost:8000/health
- **GitHub:** https://github.com/Rezzodan/CreditAI

---

## 💡 ГОРЯЧИЕ КЛАВИШИ

- `Ctrl + C` - Остановить сервер
- `Ctrl + L` - Очистить терминал (PowerShell)
- `cls` - Очистить терминал (cmd)
- `Tab` - Автодополнение команд

---

**Полная версия команд:** [КОМАНДЫ.md](КОМАНДЫ.md)

