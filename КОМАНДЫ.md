# 💻 Все команды для работы с CreditAI

**Быстрый справочник**  
**Дата:** 08.01.2026

---

## 📦 1. УСТАНОВКА И НАСТРОЙКА

### Клонирование проекта с GitHub:
```bash
git clone https://github.com/Rezzodan/CreditAI.git
cd CreditAI
```

### Создание виртуального окружения:
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### Установка зависимостей:
```bash
pip install -r requirements.txt
```

### Настройка конфигурации:
```bash
# Создать .env файл из шаблона
copy env.example .env

# Редактировать .env (откроется в блокноте)
notepad .env
```

### Инициализация базы данных:
```bash
python init_db.py
```

---

## 🤖 2. OLLAMA (AI)

### Установка Ollama:
```bash
# Скачать с https://ollama.com
# После установки проверить:
ollama --version
```

### Загрузка модели Qwen2.5:1.5b:
```bash
ollama pull qwen2.5-coder:1.5b
```

### Проверка установленных моделей:
```bash
ollama list
```

### Запуск Ollama (если не запущен):
```bash
ollama serve
```

### Тестирование модели:
```bash
ollama run qwen2.5-coder:1.5b "Привет, как дела?"
```

### Удаление модели (если нужно):
```bash
ollama rm qwen2.5-coder:1.5b
```

### Остановка Ollama:
```bash
# Windows: Ctrl+C в терминале с ollama serve
# Или через диспетчер задач
```

---

## 🚀 3. ЗАПУСК СЕРВЕРА

### Основной запуск:
```bash
# Убедитесь что виртуальное окружение активно!
python main.py
```

### Запуск через Uvicorn (альтернатива):
```bash
uvicorn api.main:app --host 0.0.0.0 --port 8000
```

### Запуск с автоперезагрузкой (для разработки):
```bash
uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
```

### Запуск на другом порту:
```bash
uvicorn api.main:app --host 0.0.0.0 --port 8080
```

### Остановка сервера:
```
Ctrl+C в терминале
```

---

## 📡 4. РАБОТА С API

### Открыть документацию API:
```
# Swagger UI
http://localhost:8000/docs

# ReDoc
http://localhost:8000/redoc
```

### Проверка здоровья сервера:
```bash
curl http://localhost:8000/health
```

### Загрузка PDF для обработки:
```bash
curl -X POST "http://localhost:8000/api/v1/process" \
  -F "file=@путь/к/файлу.pdf"
```

### Проверка статуса обработки:
```bash
curl http://localhost:8000/api/v1/status/report_id_здесь
```

### Получение статистики:
```bash
curl http://localhost:8000/api/v1/stats
```

---

## 🗄️ 5. БАЗА ДАННЫХ

### Создание новой базы данных:
```bash
python init_db.py
```

### Создание миграции (Alembic):
```bash
alembic revision --autogenerate -m "описание изменений"
```

### Применение миграций:
```bash
alembic upgrade head
```

### Откат миграции:
```bash
alembic downgrade -1
```

### Просмотр истории миграций:
```bash
alembic history
```

### Очистка базы данных:
```bash
# Удалить файл базы данных
del credit_ai.db

# Создать заново
python init_db.py
```

---

## 🐙 6. GIT КОМАНДЫ

### Инициализация репозитория:
```bash
git init
```

### Проверка статуса:
```bash
git status
```

### Добавление файлов:
```bash
# Все файлы
git add .

# Конкретный файл
git add имя_файла.py
```

### Коммит изменений:
```bash
git commit -m "Описание изменений"
```

### Проверка истории:
```bash
git log
```

### Создание ветки:
```bash
git branch имя_ветки
```

### Переключение на ветку:
```bash
git checkout имя_ветки
```

### Создание и переключение на ветку:
```bash
git checkout -b имя_новой_ветки
```

### Подключение удалённого репозитория:
```bash
git remote add origin https://github.com/username/repo.git
```

### Отправка в GitHub:
```bash
# Первый раз
git push -u origin main

# Последующие разы
git push
```

### Получение изменений:
```bash
git pull
```

### Клонирование репозитория:
```bash
git clone https://github.com/username/repo.git
```

### Просмотр удалённых репозиториев:
```bash
git remote -v
```

---

## 🐳 7. DOCKER КОМАНДЫ

### Сборка Docker образа:
```bash
docker build -t creditai .
```

### Запуск контейнера:
```bash
docker run -p 8000:8000 creditai
```

### Запуск в фоновом режиме:
```bash
docker run -d -p 8000:8000 creditai
```

### Запуск через Docker Compose:
```bash
# Запуск всех сервисов
docker-compose up

# В фоновом режиме
docker-compose up -d

# Пересборка и запуск
docker-compose up --build
```

### Остановка Docker Compose:
```bash
docker-compose down
```

### Просмотр логов:
```bash
# Все логи
docker-compose logs

# Логи конкретного сервиса
docker-compose logs creditai

# С отслеживанием
docker-compose logs -f
```

### Просмотр запущенных контейнеров:
```bash
docker ps
```

### Остановка контейнера:
```bash
docker stop container_id
```

### Удаление контейнера:
```bash
docker rm container_id
```

### Удаление образа:
```bash
docker rmi creditai
```

### Очистка неиспользуемых ресурсов:
```bash
docker system prune -a
```

---

## 🧪 8. ТЕСТИРОВАНИЕ

### Запуск всех тестов:
```bash
pytest
```

### Запуск с подробным выводом:
```bash
pytest -v
```

### Запуск конкретного теста:
```bash
pytest tests/test_api.py
```

### Запуск с покрытием кода:
```bash
pytest --cov=. --cov-report=html
```

### Тестирование API через curl:
```bash
# Проверка здоровья
curl http://localhost:8000/health

# Загрузка файла
curl -X POST -F "file=@test.pdf" http://localhost:8000/api/v1/process
```

---

## 📁 9. УПРАВЛЕНИЕ ФАЙЛАМИ

### Создание необходимых папок:
```bash
mkdir uploads output logs templates
```

### Очистка папки uploads:
```bash
# Windows
del /q uploads\*

# Linux/Mac
rm -rf uploads/*
```

### Очистка папки output:
```bash
# Windows
del /q output\*

# Linux/Mac
rm -rf output/*
```

### Просмотр размера папок:
```bash
# Windows
dir uploads /s

# Linux/Mac
du -sh uploads/
```

---

## 🔧 10. УПРАВЛЕНИЕ ЗАВИСИМОСТЯМИ

### Обновление requirements.txt:
```bash
pip freeze > requirements.txt
```

### Установка конкретной библиотеки:
```bash
pip install название_библиотеки
```

### Обновление библиотеки:
```bash
pip install --upgrade название_библиотеки
```

### Удаление библиотеки:
```bash
pip uninstall название_библиотеки
```

### Просмотр установленных пакетов:
```bash
pip list
```

### Проверка устаревших пакетов:
```bash
pip list --outdated
```

---

## 📊 11. МОНИТОРИНГ И ЛОГИ

### Просмотр логов сервера:
```bash
# Windows
type logs\creditai.log

# Linux/Mac
tail -f logs/creditai.log
```

### Просмотр последних 50 строк логов:
```bash
# Windows
powershell -command "Get-Content logs\creditai.log -Tail 50"

# Linux/Mac
tail -n 50 logs/creditai.log
```

### Очистка логов:
```bash
# Windows
del logs\*.log

# Linux/Mac
rm logs/*.log
```

### Проверка использования памяти:
```bash
# Windows
tasklist | findstr python

# Linux/Mac
ps aux | grep python
```

---

## 🔄 12. CELERY (ФОНОВЫЕ ЗАДАЧИ)

### Запуск Celery Worker:
```bash
celery -A integration.celery_tasks worker --loglevel=info
```

### Запуск с автоперезагрузкой:
```bash
celery -A integration.celery_tasks worker --loglevel=info --autoreload
```

### Запуск Flower (мониторинг):
```bash
celery -A integration.celery_tasks flower
```

### Остановка Celery:
```bash
# Ctrl+C в терминале
```

---

## 🔍 13. ОТЛАДКА

### Проверка переменных окружения:
```bash
# Windows
type .env

# Linux/Mac
cat .env
```

### Проверка портов:
```bash
# Windows
netstat -an | findstr :8000

# Linux/Mac
lsof -i :8000
```

### Проверка Python версии:
```bash
python --version
```

### Проверка pip версии:
```bash
pip --version
```

### Проверка установки библиотек:
```bash
python -c "import fastapi; print(fastapi.__version__)"
python -c "import pdfplumber; print(pdfplumber.__version__)"
```

---

## 🚀 14. БЫСТРЫЙ СТАРТ (ВСЁ ПОДРЯД)

### Первый запуск (Windows):
```bash
# 1. Активация виртуального окружения
venv\Scripts\activate

# 2. Установка зависимостей (если нужно)
pip install -r requirements.txt

# 3. Проверка Ollama
ollama list

# 4. Если модели нет - загрузить
ollama pull qwen2.5-coder:1.5b

# 5. Инициализация базы данных (первый раз)
python init_db.py

# 6. Запуск сервера
python main.py
```

### Обычный запуск (Windows):
```bash
# 1. Активация окружения
venv\Scripts\activate

# 2. Запуск сервера
python main.py

# 3. Открыть в браузере
# http://localhost:8000/docs
```

---

## 📝 15. ПОЛЕЗНЫЕ КОМАНДЫ WINDOWS

### Очистка терминала:
```bash
cls
```

### Просмотр текущей директории:
```bash
cd
```

### Переход в директорию проекта:
```bash
cd "C:\Users\user\Desktop\Cliner finance"
```

### Просмотр файлов в папке:
```bash
dir
```

### Создание файла:
```bash
type nul > имя_файла.txt
```

### Удаление файла:
```bash
del имя_файла.txt
```

### Создание папки:
```bash
mkdir имя_папки
```

### Удаление папки:
```bash
rmdir /s имя_папки
```

### Копирование файла:
```bash
copy источник.txt назначение.txt
```

### Открытие файла в блокноте:
```bash
notepad имя_файла.txt
```

### Открытие папки в проводнике:
```bash
explorer .
```

---

## 🎯 ЧАСТЫЕ СЦЕНАРИИ

### Сценарий 1: Ежедневная работа
```bash
# 1. Перейти в папку проекта
cd "C:\Users\user\Desktop\Cliner finance"

# 2. Активировать окружение
venv\Scripts\activate

# 3. Получить обновления из Git (если работаете в команде)
git pull

# 4. Запустить сервер
python main.py
```

### Сценарий 2: Обновление кода
```bash
# 1. Проверить изменения
git status

# 2. Добавить файлы
git add .

# 3. Закоммитить
git commit -m "Описание изменений"

# 4. Отправить в GitHub
git push
```

### Сценарий 3: Обновление модели AI
```bash
# 1. Удалить старую модель
ollama rm gemma:2b

# 2. Загрузить новую
ollama pull qwen2.5-coder:1.5b

# 3. Проверить
ollama list
```

### Сценарий 4: Очистка и перезапуск
```bash
# 1. Остановить сервер (Ctrl+C)

# 2. Очистить базу данных
del credit_ai.db

# 3. Пересоздать
python init_db.py

# 4. Очистить логи
del logs\*.log

# 5. Запустить заново
python main.py
```

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

### После запуска сервера:
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc
- **Health Check:** http://localhost:8000/health
- **Статистика:** http://localhost:8000/api/v1/stats

### Внешние ресурсы:
- **Ollama:** https://ollama.com
- **Qwen2.5:** https://github.com/QwenLM/Qwen2.5
- **FastAPI:** https://fastapi.tiangolo.com
- **GitHub Repo:** https://github.com/Rezzodan/CreditAI

---

## 💡 СОВЕТЫ

### Если сервер не запускается:
1. Проверьте что виртуальное окружение активно
2. Проверьте что установлены все зависимости: `pip install -r requirements.txt`
3. Проверьте что Ollama запущен: `ollama list`
4. Проверьте что порт 8000 свободен
5. Проверьте логи: `type logs\creditai.log`

### Если Ollama не работает:
1. Проверьте что Ollama установлен: `ollama --version`
2. Проверьте что модель загружена: `ollama list`
3. Попробуйте запустить вручную: `ollama serve`
4. Проверьте в .env правильный адрес: `OLLAMA_HOST=http://localhost:11434`

### Если Git не работает:
1. Проверьте что Git установлен: `git --version`
2. Проверьте статус: `git status`
3. Проверьте удалённый репозиторий: `git remote -v`
4. Проверьте ветку: `git branch`

---

## 📞 ПОДДЕРЖКА

Если что-то не работает:
1. Проверьте логи в папке `logs/`
2. Проверьте переменные окружения в `.env`
3. Проверьте документацию: [ДОКУМЕНТАЦИЯ.md](ДОКУМЕНТАЦИЯ.md)
4. Проверьте быстрый старт: [QUICK_START.md](QUICK_START.md)

---

**Обновлено:** 08.01.2026  
**Версия:** 2.0  
**Статус:** ✅ Актуально

**💡 Совет:** Сохраните этот файл в закладки для быстрого доступа!


